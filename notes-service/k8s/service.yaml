apiVersion: v1
kind: Service
metadata:
  name: notes-service
  namespace: intelligtest-namespace
spec:
  selector:
    app/component: notes-service
  ports:
    - port: 4002
      targetPort: 4002
  type: ClusterIP
