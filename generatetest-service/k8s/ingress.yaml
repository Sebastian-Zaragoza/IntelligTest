apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: generatetest-ingress
  namespace: intelligtest-namespace
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: gnts.localtest.me
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: generatetest-service
                port:
                  number: 5001
