apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
  namespace: intelligtest-namespace
spec:
  selector:
    app/component: api-gateway
  ports:
    - port: 3000
      targetPort: 3000
  type: ClusterIP
