apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: extracttext-ingress
  namespace: intelligtest-namespace
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: exts.localtest.me
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: extracttext-service
                port:
                  number: 5000
